# opencode backend connection — example / template.
# copy to `.env` and fill in the blanks. `.env` is gitignored.

OPENCODE_URL=http://localhost:4096

# fill exactly one of these:
OPENCODE_TOKEN=
OPENCODE_COOKIE=
