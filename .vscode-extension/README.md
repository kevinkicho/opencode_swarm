# Opencode Swarm — VS Code Extension

Shows opencode_swarm run status in the VS Code status bar.
Open the timeline in a webview panel.
Start a new run via the command palette.

Requires opencode_swarm running on localhost:8044.
