node_modules/
.next/
out/
dist/
.env*
!.env.example
.DS_Store
*.log
.vercel
tsconfig.tsbuildinfo
.opencode_swarm/
.dev-port
.claude/
tmp/
# Dev-phase artifacts from scripts/_* smoke runs. Event logs and
# launch payloads are too large / ephemeral to version.
demo-log/
.perf/
C:\\Users\\kevin\\AppData\\Local\\lighthouse.*
# Playwright @playwright/test artifacts. Baselines under
# tests/visual/*-snapshots/ ARE versioned (those are the ground truth);
# only diff/actual outputs from a failing visual run are noise.
test-results/
playwright-report/
